function goal(name, description, finalDate, amount, state, balance) {
    this.name = name;
    this.description = description;
    this.finalDate = finalDate;
    this.amount = amount;
    this.state = state;
    this.balance = balance;
};